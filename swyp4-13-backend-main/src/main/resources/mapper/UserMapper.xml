<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.swygbr.backend.user.mapper.UserMapper">
    <select id="findById" resultType="com.swygbr.backend.user.dto.UserDto" parameterType="java.lang.Long">
        SELECT * FROM tb_user WHERE id=#{id}
    </select>

    <select id="findByRefreshToken" resultType="com.swygbr.backend.user.dto.UserDto" parameterType="java.lang.String">
        SELECT * FROM tb_user WHERE refresh_token = #{refreshToken}
    </select>

    <insert id="save" parameterType="com.swygbr.backend.user.dto.UserDto">
        INSERT tb_user (id, email, platform, refresh_token) VALUES (#{id}, #{email}, #{platform}, #{refreshToken})
    </insert>

    <update id="update" parameterType="com.swygbr.backend.user.dto.UserDto">
        UPDATE tb_user SET email = #{email}, platform = #{platform} WHERE id = #{id}
    </update>

    <update id="updateRefreshToken" parameterType="com.swygbr.backend.user.dto.UserDto">
        UPDATE tb_user SET refresh_token = #{refreshToken} WHERE id = #{id}
    </update>
</mapper>
